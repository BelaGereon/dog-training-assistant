**Summary**
Add core Playwright flows: create exercise ? log review ? due dates update.

**Acceptance Criteria**
- e2e: create new exercise; log “ok”; verify it moves out of today (per planner).
- Keep tests idempotent; clean state between runs.

**Test Plan**
- Playwright spec(s) under /app/e2e; pass in CI.
